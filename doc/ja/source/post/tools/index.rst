リファレンス
------------

各ポスト処理ツールの詳細な使い方・オプション・出力形式を説明します。

:doc:`extract_combined`
    combined 形式で出力されたログファイルから特定の項目を取り出します。

:doc:`separateT`
    MCMCのログファイルを温度点ごとに分割します。

:doc:`summarize_each_T`
    MCMCが出力するログファイルからannealing後のレプリカの情報を取り出し、温度点ごとに集約します。

:doc:`plt_model_evidence`
    model evidence を計算・プロットします。

:doc:`plt_1D_histogram`
    1次元周辺化ヒストグラムを作成します。

:doc:`plt_2D_histogram`
    2次元周辺化ヒストグラムを作成します。

.. toctree::
   :maxdepth: 1
   :hidden:

   extract_combined
   separateT
   summarize_each_T
   plt_model_evidence
   plt_1D_histogram
   plt_2D_histogram
